# Basic-Chatbot
A rule-based chatbot that responds to greetings and simple small talk (e.g., "hello," "how are you," "bye") using keyword matching and predefined replies. Runs in a loop until the user says goodbye. Built using if-elif logic, functions, and randomized responses for variety.
